enum Permission {
  ADMIN
  USER
  LOCATIONCREATE
  LOCATIONUPDATE
  LOCATIONDELETE
  PERMISSIONUPDATE
}

type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
  resetToken: String
  resetTokenExpiry: String
  permissions: [Permission] @scalarList(strategy: RELATION)
  locations: [Location] @relation(name: "UsersLocation")
}

type GeoLocation {
  id: ID! @id
  latitude: Float!
  longitude: Float!
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
}

type Location {
  id: ID! @id
  country: String!
  city: String!
  geoLocation: GeoLocation!
  description: String
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
  user: User! @relation(name: "UsersLocation")
}

type Tour {
  id: ID! @id
  title: String!
  locations: [Location]
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
}